# Contributing

1. Create a focused branch from `main`.
2. Install the development dependencies with `make install`.
3. Add tests for every behavior change.
4. Run `make lint test` before opening a pull request.
5. Keep commits small and use an imperative subject line.

Never commit `.env`, credentials, database dumps, or generated local state.

